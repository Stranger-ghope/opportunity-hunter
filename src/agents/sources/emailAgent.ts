/**
 * Email Source Agent — removed. RSS and Reddit sources provide sufficient coverage.
 */

import type { Opportunity } from "../../types";

export async function runEmailAgent(): Promise<Opportunity[]> {
  return [];
}
